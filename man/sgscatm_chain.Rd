% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chain.R
\name{sgscatm_chain}
\alias{sgscatm_chain}
\title{Fit the three-stage anchored k-step spectral estimator (Theorem-16 chain)}
\usage{
sgscatm_chain(
  W,
  C,
  K,
  lambda = 1,
  refine = c("joint", "frozen_phi"),
  polish_em = 3L,
  readout_gn = 10L,
  refine_gn = 2L,
  max_sweeps = 100L,
  rule_tol = 0.001,
  patience = 2L,
  dz_cap = 1,
  V = NULL,
  verbose = FALSE
)
}
\arguments{
\item{W}{Numeric M x N non-negative document-term \strong{count} matrix.}

\item{C}{Numeric M x P covariate matrix (column-centred internally).}

\item{K}{Integer >= 2, number of topics.}

\item{lambda}{Pilot regularisation (Stage 1 only; the refinement uses
\eqn{\lambda=0}). Default 1.}

\item{refine}{\code{"frozen_phi"} (default, theory) or \code{"joint"} (V4).}

\item{polish_em}{Integer; pooled multinomial-EM polish iterations for the
anchored \eqn{\hat\Phi}. Default 3.}

\item{readout_gn, refine_gn}{Integer GN iterations per read-out / per refinement
sweep. Defaults 10 and 2.}

\item{max_sweeps, rule_tol, patience}{B-stationarity stop parameters
(defaults 100, 1e-3, 2).}

\item{dz_cap}{Trust-region cap on the per-document GN step norm. Default 1.}

\item{V}{Optional K x (K-1) ILR contrast matrix.}

\item{verbose}{Logical.}
}
\value{
An object of class \code{"sgscatm_chain"}: \code{Bz} (P x (K-1), oriented,
generative-scale), \code{Z} (refined scores), \code{Phi} (anchored, K x N), \code{Theta}
(M x K), \code{C_centred}, \code{W_tilde}, \code{V}, \code{K}, \code{pilot_Bz}, \code{pilot_Z}, the pilot
\code{eigenvalues}, refinement diagnostics (\code{sweeps}, \code{rule_stop}, \code{monotone_ok},
\code{n_fail}), \code{anchor_Phi}, and \code{call}.
}
\description{
The manuscript's deliverable estimator (Algorithm 1): a spectral pilot, an
anchored \strong{general-linear} orientation, and a fixed budget of \strong{least-squares}
Gauss-Newton refinement sweeps with a B-stationarity stop. Its inference is the
heteroskedasticity-robust sandwich of Lemma 17 (\code{\link[=vcov.sgscatm_chain]{vcov.sgscatm_chain()}}).
}
\details{
This differs from \code{\link[=sgscatm]{sgscatm()}}, which returns only the raw spectral pilot whose
path-coefficient block \verb{Bz = solve(C'C, C'Z*)} is the degenerate, scale-collapsed
regression of Corollary 11. Use \code{sgscatm_chain()} for coefficient estimation and
inference; the raw pilot is retained here as \code{pilot_Bz} / \code{pilot_Z} for the
subspace-recovery / collapse diagnostics.

Stages:
\enumerate{
\item \strong{Pilot} — \code{\link[=sgscatm]{sgscatm()}} internals with \code{rotate = FALSE}: unit-norm ILR
score subspace \eqn{Z^\ast}.
\item \strong{Anchored orientation} — anchor-word recovery of \eqn{\hat\Phi}
(Section 3.6(i), \link{.sg_anchor_pipeline}) with an optional pooled
multinomial-EM polish (the single deviance step); per-document Gauss-Newton
read-out on \eqn{\lVert w_i - \hat\Phi^\top f(z)\rVert^2} (trust-region
capped); \strong{general-linear} alignment of the pilot to the read-out
(\link{.sg_gl_align}).
\item \strong{k-step LS refinement} — damped per-document Gauss-Newton on the exact
\eqn{\lVert w_i - \hat\Phi^\top f(z)\rVert^2} at \eqn{\lambda=0} from the
oriented pilot, with the B-stationarity stop
(\verb{max|dBz|/rms(Bz) < rule_tol} for \code{patience} consecutive sweeps, cap
\code{max_sweeps}).
}
\code{refine = "joint"} (default) re-estimates \eqn{\Phi} (unconstrained ridge LS)
each sweep — the empirically-best "V4" variant of \verb{replication/feasibility/},
stable and monotonically improving, and the estimator the manuscript's reported
feasible numbers come from (Prop-19 alternating mode). \code{refine = "frozen_phi"}
holds \eqn{\hat\Phi} fixed — the strict regime for which the Lemma-17 sandwich is
derived — but \strong{empirically confirms Proposition 21}: the fixed-\eqn{\Phi}
criterion optimum is inconsistent, so B̂z is best after ~1 sweep and drifts away
if refined further; if used, apply the sandwich at an early-stopped iterate
(\code{max_sweeps} small). See \code{replication/certification/CHANGES.md}.
}
\seealso{
\code{\link[=vcov.sgscatm_chain]{vcov.sgscatm_chain()}}, \code{\link[=chain_se]{chain_se()}}, \code{\link[=sgscatm]{sgscatm()}}
}
