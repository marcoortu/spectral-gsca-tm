% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refine_phi.R
\name{refine_phi}
\alias{refine_phi}
\title{Re-estimate Topic-Word Distributions via One Post-Spectral Refinement Step}
\usage{
refine_phi(
  fit,
  W,
  smooth = 1e-04,
  temp = 1,
  method = c("kmeans", "em"),
  nstart = 10L,
  seed = NULL
)
}
\arguments{
\item{fit}{An \code{"sgscatm"} object returned by \code{\link[=sgscatm]{sgscatm()}}.}

\item{W}{Numeric M x N document-term matrix used to fit \code{fit}.}

\item{smooth}{Non-negative numeric. Laplace smoothing added to every entry
of the M-step estimate before row-normalising. Default \code{1e-4}.}

\item{temp}{Positive numeric. Temperature for post-M-step sharpening.
\code{1} (default) leaves the M-step distribution unchanged; values less
than \code{1} concentrate mass on the top terms.}

\item{method}{Character. Assignment method: \code{"kmeans"} (default, better
diversity/exclusivity) or \code{"em"} (LDA E-step, better coherence).}

\item{nstart}{Integer. Number of random starts for k-means
(\code{method = "kmeans"} only). Default \code{10L}.}

\item{seed}{Integer or NULL. Random seed for k-means reproducibility.}
}
\value{
The \code{fit} object with \code{Phi} replaced by the refined probability
matrix (K x N, rows sum to 1). Fields added: \code{phi_refined}, \code{phi_smooth},
\code{phi_temp}, \code{phi_method}.
}
\description{
After fitting sgscatm, the stored topic-term matrix
\eqn{\hat{\boldsymbol{\Phi}} = \mathbf{V}\hat{\boldsymbol{\Psi}}
+ \mathbf{1}_K\bar{\mathbf{w}}^\top} is constrained to a
\eqn{(K{-}1)}-dimensional affine ILR subspace. This limits topic
exclusivity and diversity because all K distributions lie on the same
low-dimensional hyperplane anchored at the corpus mean.
}
\details{
This function breaks the subspace constraint by re-estimating
\eqn{\hat{\boldsymbol{\Phi}}} as the posterior-weighted average of
document term-frequency vectors:
\deqn{\hat{\phi}^{(1)}_k \;\propto\;
  \sum_i q_{ik}\,\tilde{w}_i + \varepsilon,}
where \eqn{q_{ik}} are document-topic assignments and
\eqn{\varepsilon} is Laplace smoothing.

Two methods are available for computing the assignments \eqn{q_{ik}}:
\describe{
\item{\code{"kmeans"} (default)}{K-means on the ILR scores
\eqn{\sqrt{M}\,\hat{\mathbf{Z}}} (scaled to natural population
variance). Documents are partitioned into K clusters; each cluster
becomes one topic. This directly exploits the spectral structure of
sgscatm and produces \strong{diverse, exclusive} topics.}
\item{\code{"em"}}{One LDA-style E-step: soft assignments
\eqn{q_{ik} \propto \exp(\sum_n \tilde{w}_{in}\,
    \log\hat\phi^{(0)}_{kn})} from the current ILR-based
topic-word distribution \eqn{\hat{\boldsymbol{\phi}}^{(0)} =
    \text{softmax}(\mathbf{V}\hat{\boldsymbol{\Psi}})}. Improves
semantic coherence but has limited effect on diversity when
topics are highly similar.}
}

The document-topic proportions \eqn{\hat{\boldsymbol{\Pi}}}, path
coefficients \eqn{\hat{\mathbf{B}}_z}, ILR scores \eqn{\hat{\mathbf{Z}}},
and ILR deviations \eqn{\hat{\boldsymbol{\Psi}}} are \strong{unchanged}.

An optional temperature \eqn{\tau \in (0,1]} further sharpens the
M-step distribution by raising every entry to \eqn{1/\tau}.
}
\seealso{
\code{\link[=topic_word_dist]{topic_word_dist()}}, \code{\link[=sgscatm]{sgscatm()}}
}
