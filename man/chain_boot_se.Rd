% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sandwich.R
\name{chain_boot_se}
\alias{chain_boot_se}
\title{Full-chain document-bootstrap standard errors for the chain path coefficients}
\usage{
chain_boot_se(
  fit,
  W,
  C,
  B = 200L,
  conf = 0.95,
  seed = NULL,
  max_sweeps = 100L,
  dz_cap = 1,
  verbose = FALSE
)
}
\arguments{
\item{fit}{An \code{"sgscatm_chain"} point-estimate object.}

\item{W, C}{The original count DTM and covariate matrix the fit was built from.}

\item{B}{Integer bootstrap replicates. Default 200.}

\item{conf}{Confidence level. Default 0.95.}

\item{seed}{Integer or NULL.}

\item{max_sweeps, dz_cap}{Passed through to the per-replicate
\code{sgscatm_chain(refine = "frozen_phi", ...)}.}

\item{verbose}{Logical; progress every 50 replicates.}
}
\value{
A list with \code{se} (P x (K-1)), \code{ci_lower}, \code{ci_upper}, \code{B} (successful
replicates), and \code{boot} (P x (K-1) x B aligned replicates).
}
\description{
The calibrated \strong{feasible} SE for \code{sgscatm_chain()}. Unlike the plain Lemma-17
sandwich (\code{\link[=vcov.sgscatm_chain]{vcov.sgscatm_chain()}}) — which is calibrated only when the anchor
orientation error \eqn{\delta_\Phi} is negligible — this resamples the M
documents with replacement and \strong{re-runs the entire chain} (anchors,
orientation, frozen-\eqn{\Phi} refinement) on each resample, so the anchor
re-estimation variance that the sandwich (and the split-document jackknife)
omit is captured. Each bootstrap \eqn{\hat B_z^\ast} is permutation+sign
aligned to the \strong{point estimate} (not the truth) before the per-entry SD is
taken.
}
\seealso{
\code{\link[=sgscatm_chain]{sgscatm_chain()}}, \code{\link[=vcov.sgscatm_chain]{vcov.sgscatm_chain()}}, \code{\link[=perm_sign_align]{perm_sign_align()}}
}
